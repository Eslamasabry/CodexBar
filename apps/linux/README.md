# Linux surfaces

Linux-native surfaces follow the terminal UI. Waybar, GNOME Shell, and KDE/Plasma consumers must use CodexBar's
versioned local output and keep all provider fetching in the core/CLI.

No Linux integration may copy browser cookies or implement provider-specific authentication.
