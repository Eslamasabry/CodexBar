# Android companion

Android is intentionally a discovery surface, not an active implementation. Its future role is a read-only paired
companion with a home-screen widget, freshness indicator, and threshold/reset alerts.

Pairing, transport, background-refresh, and Android credential boundaries require a separate accepted design before
code is added. Direct provider login and storage of provider sessions on the phone are out of scope.
